name: Release crux-monitor-web

on:
  push:
    tags:
      - 'crux-monitor-web-v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build binaries
        run: cd plugins/crux-monitor/web && bun run build:binary

      - uses: actions/upload-artifact@v6
        with:
          name: binaries
          path: plugins/crux-monitor/web/bin/*

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v5
        with:
          name: binaries
          path: binaries

      - uses: softprops/action-gh-release@v2
        with:
          files: binaries/*
          generate_release_notes: true
